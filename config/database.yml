
# Configure Using Gemfile
# gem 'pg'
#
default: &default
  adapter: postgresql
  encoding: unicode
  # For details on connection pooling, see Rails configuration guide
  # http://guides.rubyonrails.org/configuring.html#database-pooling
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
  username: fine
  password: password
  host: localhost

development:
  <<: *default
  database: IVONA_development
test:
  <<: *default
  database: IVONA_development

production:
  <<: *default
  database: IVONA_production
  username: IVONA
password: <%= ENV['RSAREST_DATABASE_PASSWORD'] %>
