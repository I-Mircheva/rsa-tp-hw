<%= form_with(model: hidden_note, local: true) do |form| %>
<% if hidden_note.errors.any? %>
<div id="error_explanation">
  <h2><%= pluralize(hidden_note.errors.count, "error") %> prohibited this hidden_note from being saved:</h2>

  <ul>
    <% hidden_note.errors.full_messages.each do |message| %>
    <li><%= message %></li>
    <% end %>
  </ul>
</div>
<% end %>

<div class="field">
  <%= simple_format(form.label :your_note) %>
  <%= form.text_area :your_note, id: :hidden_note_your_note %>
</div>

<div class="actions">
  <%= form.submit %>
</div>
<% end %>
